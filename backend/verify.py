import requests

try:
    # Need to get token or login, wait... admin API requires auth
    # I can just use python to query the DB directly to mimic the API
    pass
except Exception as e:
    print(e)
